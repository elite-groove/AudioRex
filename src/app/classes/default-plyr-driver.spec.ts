import { DefaultPlyrDriver } from './default-plyr-driver';

describe('DefaultPlyrDriver', () => {
  it('should create an instance', () => {
    expect(new DefaultPlyrDriver()).toBeTruthy();
  });
});
